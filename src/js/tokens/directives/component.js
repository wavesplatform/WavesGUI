(function () {
    'use strict';

    angular
        .module('app.tokens')
        .component('wavesTokens', {
            controller: 'tokenCreateController',
            templateUrl: 'tokens/component'
        });
})();
