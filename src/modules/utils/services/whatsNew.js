(function () {
    'use strict';

    /**
     *
     */
    const factory = function () {

        return Object.create(null);
    };

    factory.$inject = [];

    angular.module('app.utils').factory('whatsNew', factory);
})();
